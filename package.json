{
  "name": "gpt-image-prompt-maker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "API-free GPT Image prompt maker with structured ChatGPT expansion.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
